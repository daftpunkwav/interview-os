修复审查发现的 5 项问题。基于探索证据(rewrites 为死代码、前端无同源代理消费、eslint-config-next 15.5 为 legacy 格式、@eslint/eslintrc 3.3.6 已装、pip-audit 2.10.1 支持 --ignore-vuln、代码无 3.12+ 专属语法),方案如下:

## 改动清单

### 1. 前端 ESLint 配置缺失 + `next lint` 废弃 (P1)
**新建 `frontend/eslint.config.mjs`**(eslint v9 flat config,用 FlatCompat 包装 legacy 的 eslint-config-next):
- `compat.extends("next/core-web-vitals", "next/typescript")`
- ignores: `.next/**`、`node_modules/**`、`next-env.d.ts`、`vitest.config.ts`、构建产物
**修改 `frontend/package.json`**:`"lint": "next lint"` → `"lint": "eslint ."`
**验证**:`npm run lint` 非交互跑通,零错误(若有规则违规,只修违规点,不降级规则)。

### 2. 前端 CI 缺 lint/test (P1)
**修改 `.gitlab-ci.yml` frontend job** script 增加:
```yaml
- npm run lint
- npm run test
```
(vitest 现有 3 文件 33 用例,已实测全绿)

### 3. rewrites 死代码 + 文档漂移 (P2)
**修改 `frontend/next.config.js`**:删除 `rewrites()` 块(`/api/:path*`、`/health` 两条)。**保留** `backendOrigin`/`connectSrc` 派生与 CSP headers(它们是活的:api.ts 直连 + CSP connect-src)。
**修改 `README.md:208`**:`src/app/` 下的 `api/ # 本地 Next 路由代理` 条目删除(该目录实际不存在),如结构树中有页面目录则如实标注。
**修改 `frontend/.env.example:6`**:注释从 "Next.js 会把 /api/* 代理到这里" 改为 "前端 api.ts 直连后端(不走 Next 代理)"。

### 4. Python 版本对齐 (P2)
**修改 `.gitlab-ci.yml:8`**:`python:3.11-slim` → `python:3.12-slim`(与 mypy `python_version=3.12` 对齐;numpy 类型存根需要 3.12+,而代码经搜索确认无 3.12+ 专属语法,CI 用 3.12 验证类型检查 target 无漂移)。`requires-python >=3.11` 保持不变(库声明仍兼容)。
**修改 `backend/pyproject.toml:17` 注释**:更新说明为 "mypy 3.12 与 CI python:3.12 对齐"。

### 5. 依赖审计门禁 (P2)
**修改 `.gitlab-ci.yml` backend job**:
```yaml
- pip-audit -r requirements.txt --ignore-vuln PYSEC-2026-311
```
去掉 `|| true`(chromadb PYSEC-2026-311 精确忽略,新漏洞会使 CI 变红;实施时实测 `--ignore-vuln` 参数,若该版本不支持则回退 `|| true` 并记录)。
**前端 npm audit 维持 `|| true`**(brace-expansion 无漏洞 alias 可精确忽略,强制阻断会使 CI 持续红),完善注释说明原因。

### 不动的部分(明确标注)
- **Node 版本**(CI node:20 / 本地 24):CI 基线固定 node:20 合理,非代码缺陷,README 已注明 Node 18+。仅此说明,不改。
- **start.sh/start.ps1 端口逻辑**:已确认合规,TOCTOU 属低概率已知接受。
- **CORS / 暴露面 / 密钥管理**:审查全绿,无改动。

## 验证清单(实施后逐项运行)
1. `cd frontend && npm run lint` → 零错误退出
2. `cd frontend && npm run test` → 33 用例全绿
3. `cd frontend && npx tsc --noEmit` → 退出码 0
4. `cd backend && python -m pytest tests/test_security.py tests/test_error_codes.py -q` → 通过(CI 前端/后端均用 node:20/python:3.12 镜像模拟,本地为 3.14/24,类型检查 target 已对齐)
5. `pip-audit -r requirements.txt --ignore-vuln PYSEC-2026-311` 在 CI 环境(3.12 镜像)外先本地跑通,确认参数行为后落盘
6. `cd backend && python -m mypy app/ --ignore-missing-imports` → 通过
7. 复检 `grep -rn "fetch(\"/api\|fetch('/api"` 确认无残留同源相对调用(应无)

## 风险与说明
- `eslint.config.mjs` 是本次唯一新增文件;FlatCompat 包装是 eslint-config-next 15.5(legacy 格式)在 eslint 9 flat config 下的标准迁移路径。
- 删除 rewrites 后 `/api/*` 不再有同源兜底,但前端代码已全部直连(已验证 4 处 fetch 均为绝对 URL),无行为变化。
- 所有改动均为配置/文档/CI 层,不触碰后端路由、前端业务代码、CORS 逻辑。