# 面试官 3D 人像（TalkingHead 兼容 GLB）

本地同源加载，避免 CDN 失败回退成 CSS 卡通。

| 文件 | avatar_id | 来源 / 说明 |
|------|-----------|-------------|
| `professional_male.glb` | `professional_male` | TalkingHead `avatarsdk.glb`（body M） |
| `senior_male.glb` | `senior_male`、`strict_expert` | TalkingHead `mpfb.glb`（体型更大，约 35MB） |
| `gentle_female.glb` | `gentle_female` | TalkingHead `brunette.glb`（body F，CC BY-NC） |
| `hr_female.glb` | `hr_female` | TalkingHead `avaturn.glb`（body F） |
| `young_female.glb` | `young_female` | TalkingHead `brunette-t.glb`（body F） |

非商用示例资源，正式上线请替换为自有授权模型。
