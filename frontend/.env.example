# InterviewOS 前端环境变量示例（生产环境必须设置）
# 复制为 .env.local 并填入实际值；不要提交真实配置到仓库。

# 后端 REST API 基础地址（Next.js 会把 /api/* 代理到这里）
NEXT_PUBLIC_API_BASE=http://localhost:8000

# WebSocket 直连地址（绕过 Next 代理，避免 WS upgrade 兼容问题）
NEXT_PUBLIC_WS_URL=ws://localhost:8000

# 流式接口（SSE）的直连地址，必须与 NEXT_PUBLIC_API_BASE 同源；单独设置以绕过代理
NEXT_PUBLIC_STREAM_API_BASE=http://localhost:8000
